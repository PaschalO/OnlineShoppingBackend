# This config was automatically generated from your source code
# Stacks detected: cicd:github-actions:.github/workflows,deps:node:.
version: 2.1

orbs:
  node: circleci/node@5

executors:
  node-executor:
    docker:
      - image: circleci/node:18.16.1

jobs:
  build:
    executor: node-executor
    steps:
      - checkout
      - node/install-packages:
          pkg-manager: npm
      - run:
          name: Install backend dependencies
          command: npm install
      - run:
          name: Backend build
          command: npm run build

  # Example deploy job (commented out by default)
  deploy:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: Deploy
          command: npm run deploy

workflows:
  build-and-test:
    jobs:
      - build
      # Uncomment the deploy job if needed
      # - deploy:
      #     requires:
      #       - build
